name: .NET Release

concurrency:
  group: ${{ github.workflow }}-${{ github.event_name }}-${{ github.ref }}
  cancel-in-progress: true

on:
  workflow_dispatch:  # Manuel olarak çalıştırabilmek için.
  push:
    branches:
      - main          # Ana branch'e push yapıldığında çalışır.
    paths-ignore:
      - "**/*.md"

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: 🚀 Repository’yi Kopyala
        uses: actions/checkout@v4.2.2

      - name: 🛠️ .NET SDK’yı Kur
        uses: actions/setup-dotnet@v4.3.0
        with:
          dotnet-version: 9.0.x

      - name: 📦 Bağımlılıkları Yükle
        run: dotnet restore

      - name: 🔨 Projeyi Tek Bir EXE Olarak Derle
        run: dotnet publish -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeAllContentForSelfExtract=true -o publish

      - name: 🗜️ "ShadeBox.exe" Dosyasını 7z ile Arşivle
        run: |
          cd publish
          7z a ../ShadeBox.7z ShadeBox.exe

      - name: 🏷️ Versiyon Numarası Al (Windows PowerShell ile)
        id: version
        shell: pwsh
        run: |
          $version = Get-Date -Format "yyyy.MM.dd-HH:mm:ss"
          echo "VERSION=$version" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8

      - name: 🔥 Mevcut "latest" Release'i Sil
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        run: |
          $release = gh release view latest --json id --jq '.id' 2>$null
          if ($release) {
            gh release delete latest --yes
          }
        shell: pwsh

      - name: 🚀 GitHub Releases'a Yükle
        uses: softprops/action-gh-release@v2.2.1
        env:
          GITHUB_TOKEN: ${{ secrets.GH_PAT }}
        with:
          tag_name: latest
          name: "Güncel Sürüm"
          body: "${{ env.VERSION }} - ${{ github.sha }} için otomatik oluşturulan sürüm."
          draft: false
          prerelease: false
          files: ShadeBox.7z